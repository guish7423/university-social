name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    env:
      VITE_BASE: /university-social/
      VITE_API_BASE: ${{ vars.VITE_API_BASE || 'http://localhost:8081/api/v1' }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: admin/package-lock.json

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Build Frontend (uni-app H5)
        working-directory: frontend
        run: |
          pnpm install
          pnpm run build:h5
          mkdir -p ../dist
          cp -r dist/build/h5/* ../dist/ 2>/dev/null || cp -r dist/* ../dist/ 2>/dev/null || true

      - name: Build Admin
        working-directory: admin
        run: |
          npm ci
          npx vite build
          mkdir -p ../dist/admin
          cp -r dist/* ../dist/admin/

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
